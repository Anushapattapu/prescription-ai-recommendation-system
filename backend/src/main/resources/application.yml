spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true

server:
  port: 8080

gemini:
  api-key: ${GEMINI_API_KEY}

app:
  use-mock: true